# Propose major and minor version updates monthly, grouping minor releases
# by ecosystem and capping open version PRs at two each. Review major
# upgrades individually and run native checks where needed (docs/testing.md).
version: 2

updates:
  - package-ecosystem: cargo
    directories:
      - /desktop/src-tauri
      - /desktop/setup/src-tauri
    schedule:
      interval: monthly
    open-pull-requests-limit: 2
    allow:
      - dependency-name: "*"
        update-types: [version-update:semver-major, version-update:semver-minor]
    groups:
      rust-minor:
        update-types: [minor]
    ignore:
      # Pinned together with scripts/sherpa-runtime.lock; the runtime and the
      # bindings move in one change.
      - dependency-name: sherpa-onnx
      - dependency-name: sherpa-onnx-sys

  - package-ecosystem: npm
    directories:
      - /desktop
      - /site
    schedule:
      interval: monthly
    open-pull-requests-limit: 2
    allow:
      - dependency-name: "*"
        update-types: [version-update:semver-major, version-update:semver-minor]
    groups:
      js-minor:
        update-types: [minor]
    ignore:
      # React 19.3 adds about 30 kB to the startup chunk of every window and
      # nothing the app uses; update it when a feature needs it.
      - dependency-name: react
        update-types: [version-update:semver-minor]
      - dependency-name: react-dom
        update-types: [version-update:semver-minor]
      - dependency-name: "@types/react"
        update-types: [version-update:semver-minor]
      - dependency-name: "@types/react-dom"
        update-types: [version-update:semver-minor]

  - package-ecosystem: uv
    directories:
      - /tests/ui
      - /scripts/macos-dmg
    schedule:
      interval: monthly
    open-pull-requests-limit: 2
    allow:
      - dependency-name: "*"
        update-types: [version-update:semver-major, version-update:semver-minor]
    groups:
      python:
        update-types: [minor]

  # Keeps the SHA pins current; the trailing version comments are updated
  # with them.
  - package-ecosystem: github-actions
    directories:
      - /
      - /.github/actions/*
    schedule:
      interval: monthly
    open-pull-requests-limit: 2
    allow:
      - dependency-name: "*"
        update-types: [version-update:semver-major, version-update:semver-minor]
    groups:
      actions:
        update-types: [minor]
